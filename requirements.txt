# Runtime and verification dependencies for the Self-Expanding Corpus Engine.
# HTTP uses the stdlib-only, DNS-pinned corpus_live_fetch transport. DefusedXML
# hardens untrusted XML; jsonschema verifies the published schema contracts.
defusedxml>=0.7.1,<1
jsonschema>=4.18,<5
