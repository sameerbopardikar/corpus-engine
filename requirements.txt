# Runtime dependencies for the Self-Expanding Corpus Engine.
# HTTP uses the stdlib-only, DNS-pinned corpus_live_fetch transport. The sole
# third-party runtime dependency hardens untrusted remote and uploaded XML.
defusedxml>=0.7.1,<1
